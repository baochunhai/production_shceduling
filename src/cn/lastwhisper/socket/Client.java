package cn.lastwhisper.socket;

import java.io.PrintWriter;
import java.net.InetAddress;
import java.net.Socket;

public class Client {
	/*
	 * public static void main(String[] args) { send("333"); }
	 * 
	 * public static String send(String id) { try {
	 * 
	 * Socket socket = new Socket("192.168.1.8", 5000); PrintWriter pw = new
	 * PrintWriter(socket.getOutputStream(), true); pw.println(id +
	 * ",insert"+InetAddress.getLocalHost()); } catch (Exception e) {
	 * e.printStackTrace(); return "defeat"; } return "ok"; }
	 */
}
